name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "My First Github Action"

      - name: List files
        run: ls -la

      - name: Read file
        run: cat README.md